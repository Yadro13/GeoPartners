# Backlog тестового середовища GeoPartners

Цей файл містить технічні та продуктові роботи, які можна виконувати у staging без додаткових рішень замовника. Питання, що потребують погодження, ведуться окремо у `CUSTOMER_QUESTIONS.md`.

Пріоритетний backlog за результатами зустрічі 5 серпня та уточнень 6 серпня 2026 року ведеться у `MEETING_BACKLOG_2026-08-05.md`. M1, M2, M3 і базову частину M5 завершено у staging. Для M4 реалізовано технічний фундамент, а розклад, строк зберігання, KPI та користувацький перегляд погоджено у Q-009 і можна реалізовувати.

## Правила просування

- Уся поточна розробка виконується у гілці `staging`.
- Railway staging автоматично розгортає гілку `staging`.
- Гілка `main` та Railway production не змінюються до погодженого релізу.
- Перенесення у `main` виконується після проходження перевірок відповідного батча у staging.
- Дані PostgreSQL і Bucket переносяться у production окремою контрольованою процедурою перед запуском.

## Батч S0. Цілісність репозиторію і staging deploy

- [x] Обмежити правило ігнорування тестових файлів кореневою папкою `/plots/`.
- [x] Додати до Git API створення, редагування, видалення та завантаження документів ділянок.
- [x] Підключити Railway staging до гілки `staging`.
- [x] Перевірити автоматичне розгортання контрольним комітом.
- [x] Перевірити CRUD ділянки через реальні API та PostgreSQL у staging.

## Батч S1. Реальні інтеграційні тести

- [x] Додати E2E-сценарій із реальною PostgreSQL замість лише `/ui-preview` і `localStorage`.
- [x] Перевірити реєстрацію, підтвердження email, схвалення адміністратором та призупинення доступу.
- [x] Перевірити серверні права `user` і `admin` для CRUD, імпорту, категорій, аудиту та відновлення.
- [x] Перевірити ізоляцію робочої і тестової областей та очищення тимчасових даних тестової області.
- [x] Забезпечити очищення тимчасових користувачів і даних після тесту.

Ручний контрольний CRUD-сценарій у `sandbox` виконано 24 липня 2026 року: create, reload/read, update, audit і cleanup підтверджено на Railway staging. Батч S1 залишається відкритим до автоматизації ізольованої тестової ідентичності та каналів підтвердження.

Автоматизований сценарій описано у `STAGING_INTEGRATION_TEST.md` і успішно виконано у Railway staging 24 липня 2026 року. Перевірка завершилася `PASS`, після чого runner підтвердив відсутність тимчасових користувачів, ділянок та аудиту.

## Батч S2. Захист імпорту і документів

- [x] Застосувати однакові обмеження кількості та розміру файлів до inspect і final import.
- [x] Перевіряти сигнатуру PDF та фактичний формат JSON/GeoJSON, а не лише розширення.
- [x] Обмежити inspect API дозволом адміністратора на імпорт.
- [x] Додати контроль загального розміру пакета і передбачувані повідомлення для пошкоджених файлів.
- [x] Перевірити очищення вже завантажених Bucket-об'єктів при помилці транзакції.
- [x] Додати імпорт ZIP як безпечне розпакування у звичайний пакет GeoJSON/PDF.

## Батч S3. Надійність інтерфейсу

- [x] Додати глобальні error і not-found екрани та безпечний повтор операції.
- [x] Перевірити loading, empty та error стани основних desktop/mobile сценаріїв.
- [x] Провести accessibility-перевірку клавіатури, фокуса, діалогів і контрасту.
- [x] Повторити візуальну перевірку desktop і mobile після реальних API-тестів.

## Батч S4. Експлуатація staging

- [x] Додати структуровані серверні журнали для імпорту, auth, worker і backup без персональних даних та секретів.
- [x] Додати контроль невідправлених повідомлень і помилок notification outbox.
- [x] Зафіксувати перевірку останнього успішного backup та restore drill у приймальному чек-листі.
- [x] Підготувати dry-run процедуру перенесення PostgreSQL і Bucket зі staging у production без виконання переносу.

## Батч S5. Технічний фундамент історичних знімків

- [x] Додати версіонований формат повного знімка ділянок, категорій, етапів і зв'язків із результатами робіт.
- [x] Формувати знімок атомарно в узгодженому стані транзакції та зберігати контрольний SHA-256 hash.
- [x] Заборонити зміну вже створеного знімка на рівні PostgreSQL; видалення залишити лише для майбутньої погодженої retention-політики.
- [x] Додати захищені API створення, переліку та читання з ізоляцією робочої і тестової областей.
- [x] Додати таблицю знімків до перевірки нових backup-архівів, зберігши можливість перевіряти й відновлювати старі архіви.
- [ ] Додати погоджений розклад Friday 23:00 Europe/Kyiv, 90-денний retention, admin-only read-only карту/звіт, підсумки з дельтою та PDF.

## Відкладено до відповіді замовника

- Домен, DNS та остаточний Google OAuth callback.
- Фактичне перенесення staging-даних у production.
