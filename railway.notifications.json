{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "npm run notifications:dispatch",
    "cronSchedule": "*/5 * * * *",
    "restartPolicyType": "NEVER"
  }
}
